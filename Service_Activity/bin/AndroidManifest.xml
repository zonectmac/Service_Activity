<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.service_activity"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity2"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="com.example.service_activity.MsService" >
            <intent-filter>
                <action android:name="com.example.communication.MSG_ACTION" />
            </intent-filter>
        </service>
        <service android:name="com.example.service_activity.CopyOfMsService" >
            <intent-filter>
                <action android:name="com.example.communication.MSG_ACTION2" />
            </intent-filter>
        </service>
    </application>

</manifest>